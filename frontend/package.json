{
  "name": "frontend",
  "private": true,
  "scripts": {
    "build": "npx spago bundle --bundle-type app --outfile dist/app.js && cp -r public/. dist/"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "purescript": "^0.15.15",
    "spago": "^1.0.3"
  }
}

services:
  app:
    build: .
    ports:
      - "1230:1230"
    volumes:
      - data:/data

  # MCP Server（未実装・実装後に有効化）
  # mcp-server:
  #   build: ...
  #   depends_on:
  #     - app

volumes:
  data:
